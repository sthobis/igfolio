{
  "scripts": {
    "now-build": "next build && next export -o dist"
  },
  "dependencies": {
    "isomorphic-unfetch": "^3.0.0",
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  }
}
